extends ../layout

block content
  div.container
    if posts.length > 0
      table.table.w-100.p-5
        thead
          tr
            th Title
            th Actions
        tbody
          for post in posts
            tr
              td= post.title
              td empty
    else if posts.length === 0
      p empty
